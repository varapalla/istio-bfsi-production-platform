apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: payments-egress
  namespace: payments
  labels:
    app.kubernetes.io/part-of: istio-bfsi-production-platform
    app.kubernetes.io/component: gateway-resilience
spec:
  minAvailable: 1
  selector:
    matchLabels:
      gateway.networking.k8s.io/gateway-name: payments-egress