apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: cart-default-deny
  namespace: cart
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: cart-waypoint
  action: ALLOW