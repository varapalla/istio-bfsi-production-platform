apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bfsi-network
  namespace: argocd
spec:
  project: bfsi-platform

  source:
    repoURL: https://github.com/varapalla/istio-bfsi-production-platform.git
    targetRevision: main
    path: phases/phase-05-network-security

  destination:
    server: https://kubernetes.default.svc
    namespace: payments

  syncPolicy:
    automated:
      selfHeal: true
      prune: false

    syncOptions:
      - CreateNamespace=true